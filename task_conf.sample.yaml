basePath: "E:/workspace/project/test"
basePackage: com.songfeifan.test
author: joker
beanName: User
reqMapping: user
db:
  host: localhost
  port: 3306
  db_name:
  user:
  password:
  table:
file:
  mkdir: true
  # YES/NO/SKIP
  overwrite: NO
render:
  # out, file out path; real_path, to real path; console, to console;
  out: out
tpl:
  basePath: ./tpl_local